---
description: Analyze a feature and create a plan plus task contracts.
agent: leader
---

Analyze and plan the following feature using the manual Engineering OS flow.

You must:

1. inspect the repository, Git state, `AGENTS.md`, and available skills;
2. read `.opencode/model-profiles.md`;
3. create a concise plan under `.opencode/plans/`;
4. create one or more valid YAML task contracts under `.opencode/tasks/`;
5. detect project-appropriate verification commands;
6. define task dependencies, writable scopes, and safe parallel groups;
7. recommend `/develop-deepseek`, `/develop-glm`, or `/develop` for each task;
8. verify the artifact files exist;
9. return only artifact paths, order, blockers, and the exact next manual step.

Do not implement production code, create branches, commit, push, review, or
integrate.

Feature request:

$ARGUMENTS
